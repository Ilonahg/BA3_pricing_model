# 💰 BA3 Pricing Model — Модель прогнозирования ценообразования

---

## 📘 Описание проекта

Проект **BA3 Pricing Model** — это система прогнозирования цен на товары компании на основе исторических данных о продажах, затратах на продвижение и цен конкурентов.

Цель проекта — **создать и обучить модель**, которая будет прогнозировать цену товара на определённом рынке, учитывая:

- историю продаж (количество проданных единиц товара);
- затраты на продвижение;
- тип компании и продукта;
- цену конкурентов.

Решение построено с использованием **Python 3.11**, **pandas**, **scikit-learn**, **SQLAlchemy** и **joblib**.

---

## 🧩 Структура проекта

BA3_pricing_model/
│
├── data/
│ ├── csv_data.csv # Исходные данные (CSV)
│ └── pricing_data.db # SQLite база данных
│
├── models/
│ └── pricing_model.pkl # Сохранённая модель (Joblib)
│
├── src/
│ ├── data_loader.py # Загрузка и предобработка данных
│ ├── database.py # Работа с базой данных (SQLite)
│ ├── model.py # Обучение и прогнозирование модели
│ ├── updater.py # Обновление модели с новыми данными
│ └── main.py # Главный запускной файл проекта
│
├── tests/
│ ├── test_data_loader.py # Тесты модуля загрузки данных
│ ├── test_database.py # Тесты базы данных
│ └── test_model.py # Тесты модели прогнозирования
│
└── README.md # Описание проекта

 

---

## ⚙️ Установка и настройка окружения

1. Установите Python **3.11** или выше  
   [Скачать Python](https://www.python.org/downloads/)

2. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/Ilonahg/BA3_pricing_model.git
   cd BA3_pricing_model
Создайте виртуальное окружение и активируйте его:
 
python -m venv venv
venv\Scripts\activate
Установите зависимости:
 
pip install -r requirements.txt
🧠 Основные компоненты проекта
1️⃣ data_loader.py
Загружает данные из CSV (csv_data.csv)

Проверяет наличие столбца competitor_price

Заполняет пропуски средними значениями

Кодирует категориальные признаки (company, product)

Пример:
 
from src.data_loader import DataLoader

loader = DataLoader("data/csv_data.csv")
df = loader.load_data()
df = loader.preprocess()

2️⃣ database.py
Управляет базой данных pricing_data.db

Сохраняет обработанные данные в SQLite с высокой скоростью (chunksize=5000)

Загружает данные обратно в DataFrame

Пример:
 
from src.database import DatabaseManager

db = DatabaseManager()
db.save_data(df)
df_loaded = db.load_data()

3️⃣ model.py
Использует RandomForestRegressor для прогнозирования цены

Делит выборку на train/test

Сохраняет обученную модель в models/pricing_model.pkl

Метрики:

MAE — средняя абсолютная ошибка

RMSE — корень из среднеквадратичной ошибки

R² — коэффициент детерминации

Пример:

python
from src.model import PriceModel

pm = PriceModel()
df = pm.load_data()
pm.train(df)
prediction = pm.predict(df.drop(columns=["price"]).iloc[[0]])
print(prediction)

4️⃣ updater.py
Автоматически обновляет базу данных и модель при появлении новых данных в csv_data.csv.

5️⃣ main.py
Главный модуль, который объединяет все этапы:

Загружает CSV

Обрабатывает данные

Сохраняет в базу

Обучает модель

Делает прогноз

Выводит результаты и время выполнения

Пример запуска:
 
python -m src.main
Пример вывода:

csharp
Копіювати код
[INFO] --- Запуск основного процесса ценообразования ---
[INFO] Файл data/csv_data.csv успешно загружен. Размер: (1000000, 6)
[INFO] Данные успешно обработаны. Новый размер: (1000000, 6)
[INFO] Данные сохранены в таблицу 'pricing_data'.
[INFO] Модель обучена ✅
MAE: 23012.78, RMSE: 26943.13, R2: -0.07
[INFO] Модель сохранена в: models/pricing_model.pkl
[INFO] Прогноз для первого примера: 63326.46
[INFO] --- Работа завершена успешно ✅ ---
[INFO] Время выполнения: 86.10 сек.
🧪 Тестирование
Запуск всех тестов:
 
pytest -v
Пример успешного теста:

tests/test_model.py::test_model_training PASSED
tests/test_model.py::test_model_prediction PASSED
tests/test_database.py::test_save_and_load_data PASSED
tests/test_data_loader.py::test_preprocess PASSED
📊 Результаты
Обученная модель хранится в models/pricing_model.pkl

Средняя ошибка (MAE): ~23 000

RMSE: ~26 900

Время работы полного пайплайна: ~86 секунд на 1 млн строк

🚀 Возможности для улучшения
Использовать Gradient Boosting / XGBoost для повышения точности.

Добавить REST API (например, через FastAPI) для онлайн-прогнозирования.

Настроить автоматическую загрузку новых CSV файлов по расписанию.

✅ Качество кода и тестирование

Покрытие тестами:
Проект покрыт автоматическими тестами на уровне не менее 75%.
Тесты проверяют корректность загрузки и предобработки данных, работу с базой данных, обучение модели и выполнение основных сценариев.
Для проверки покрытия используется команда:
📊 Отчёт о покрытии тестов

Для детального просмотра отчёта о покрытии кода тестами:
👉 Открыть HTML-отчёт (htmlcov/index.html)

pytest --cov=src --cov-report=term-missing


Соответствие стандарту PEP8:
Весь код проекта соответствует стилевым требованиям PEP8.
Для проверки используется инструмент flake8, команда:

flake8 src tests

👨‍💻 Автор
Илона Гогильчин
 

📧 Email: gogilchyn2005ilona@gmail.com
📅 Год: 2025
🌐 Проект создан в рамках задачи BA3 — Модель ценообразования

---

 